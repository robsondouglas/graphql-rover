supergraph:
  listen: 0.0.0.0:4000
cors:
  origins:
    - http://localhost:5000
    - http://localhost:5001
    - http://localhost:5002
    - http://localhost:5003
headers:
  all:
    request:
      - propagate:
          matching: .*
# authentication:
#  jwt:
#    jwks: 
#      - url: https://cognito-identity.amazonaws.com/.well-known/jwks_uri